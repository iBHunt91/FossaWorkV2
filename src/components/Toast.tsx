import React from 'react';

// This component is disabled since we're using ToastContext for notifications
// Keeping it as a stub to prevent import errors
const Toast: React.FC = () => {
  return null;
};

export default Toast; 